<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shaoguang.core.model.CommonSequence">
	<select id="getActionConf" parameterType="string" resultType="action">
        SELECT t.*
          FROM z_action t 
         WHERE t.actionurl = #{actionurl}
    </select>
</mapper>
